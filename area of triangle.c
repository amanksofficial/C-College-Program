#include <stdio.h>

int main() 
{
 double base, height, area;
  double a = 0.5;
 printf("Enter the base of the triangle :-");
 scanf("%lf", &base);
 
printf("Enter the height of the triangle :-");
 scanf("%lf", &height);
 
 area = a*base*height;
printf("the area of the tringle is%lf:-", area);

    return 0;
}
